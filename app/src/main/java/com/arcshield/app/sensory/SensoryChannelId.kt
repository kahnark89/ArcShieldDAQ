package com.arcshield.app.sensory

/** Identifies the five operator sensory channels modeled by the CIAER+ Pre-ENV layer. */
enum class SensoryChannelId {
    ACOUSTIC,
    VIBRATION,
    VISUAL,
    OLFACTORY,
    THERMAL
}
